import type { IUser } from '$lib/interfaces';
import type { AuthModel } from 'pocketbase';
import { writable, type Writable } from 'svelte/store'

/* export const currentUser = writable<Record | Admin | null>() */
/* export const userProfileStore: Writable<IUser | undefined | null> = writable(null);

export const currentUserStore: Writable<AuthModel | undefined | null> = writable(null); */
